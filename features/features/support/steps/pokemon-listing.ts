/*
import {Given, Then} from "@cucumber/cucumber"
import {expect} from "@playwright/test"

Given("I'm on the home page", async function () {
  await this.navigateTo("/")
});

Given("I'm Clueless Carl", function () {
  return true
});

Then("I expect to see Kyle's full name and the word {string}", async function (interest: string) {
  await expect(this.page.locator("h1")).toContainText("Kyle Coberly")
  await expect(this.page.locator("#about")).toContainText(interest)
});
*/
